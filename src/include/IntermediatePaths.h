#include <unordered_map>
#include <vector>
#include <utility>
class IntermediatePaths{
  private:
    // std::unordered_map<std::pair<int, int>, std::vector<int>> paths; // TODO: fix compile error
  public:

    void add_path(std::pair<int, int> , std::vector<int>);

};